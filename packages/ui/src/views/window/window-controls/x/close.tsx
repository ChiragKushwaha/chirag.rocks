import styles from '../utils';

const Close = () => {
  return (
    <button
      className="w-[12px] h-[12px] rounded-full bg-[#FF5F57]"
      style={styles}
    ></button>
  );
};

export default Close;
