import styles from '../utils';

const Minimize = () => {
  return (
    <button
      className="w-[12px] h-[12px] rounded-full bg-[#FEBC2E]"
      style={styles}
    ></button>
  );
};

export default Minimize;
