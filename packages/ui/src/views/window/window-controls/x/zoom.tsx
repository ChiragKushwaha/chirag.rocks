import styles from '../utils';

const Zoom = () => {
  return (
    <button
      className="w-[12px] h-[12px] rounded-full bg-[#28C840]"
      style={styles}
    ></button>
  );
};

export default Zoom;
